<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Вы зарегистрировались!</title>
    <style>
        /* Стили для страницы */
        body, html {
            height: 100%;
            margin: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #ff7e5f, #feb47b);
            display: flex;
            justify-content: center;
            align-items: center;
            color: #333;
        }

        .error-container {
            background: white;
            padding: 40px 60px;
            border-radius: 12px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
            text-align: center;
            width: 300px;
        }

        h1 {
            font-size: 24px;
            margin-bottom: 20px;
            color: #ff4757;
        }

        p {
            font-size: 18px;
            margin-bottom: 30px;
            color: #555;
        }

        .error-message {
            color: #008000;
            font-weight: bold;
            animation: shake 0.5s ease;
        }


        a {
            display: inline-block;
            margin-top: 20px;
            padding: 10px 20px;
            background-color: #008000;
            color: white;
            text-decoration: none;
            border-radius: 6px;
            transition: background-color 0.3s ease;
        }

            a:hover {
                background-color: #00FF7F;
            }
    </style>
</head>
<body>
    <div class="error-container">
        <h1>Вы зарегистрировались!</h1>
        <a href="@Url.Action("Login", "auth")" class="back-button">Вернуться на страницу входа</a>
    </div>
</body>
</html>
